<!-- Heading -->
<% if (title) { %>
    <h1>
        <%= title %>
    </h1>

    <% } else { 
        res.redirect('/') 
    } %>

    <!-- The message -->
     <%# messages() %>

    <!-- Inventory items display -->
     <%- grid %>